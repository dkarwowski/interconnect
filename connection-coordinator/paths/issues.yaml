AllIssues:
  get:
    description: Lists Issues in a given project and location.
    operationId: ListIssues
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - description: 'Optional. The maximum number of issues to return. The service may return fewer than

          this value. If unspecified, at most 50 issues will be returned. The maximum

          value is 1000; values above 1000 will be coerced to 1000.'
        in: query
        name: pageSize
        schema:
          format: int32
          type: integer
      - description: 'Optional. A page token, received from a previous `ListIssues` call.

          Provide this to retrieve the subsequent page.'
        in: query
        name: pageToken
        schema:
          type: string
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: "../schemas/issues.yaml#/ListIssuesResponse"
        description: Successful operation
    security: []
    tags:
      - Issues
  post:
    description: Creates a new Issue in a given project and location.
    operationId: CreateIssue
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - description: 'Required. The ID to use for the issue, which will become the final component of the issue''s resource name.'
        in: query
        name: issueId
        required: true
        schema:
          type: string
    requestBody:
      content:
        application/json:
          schema:
            $ref: '../schemas/issues.yaml#/Issue'
      description: Required. The resource being created
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: '../schemas/issues.yaml#/Issue'
        description: Successful operation
    security: []
    tags:
      - Issues
OneIssue:
  get:
    description: Gets details of a single Issue.
    operationId: GetIssue
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - $ref: "../parameters/_index.yaml#/parameters/issue"
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: '../schemas/issues.yaml#/Issue'
        description: Successful operation
    security: []
    tags:
      - Issues
CloseIssue:
  post:
    description: Closes an Issue.
    operationId: CloseIssue
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - $ref: "../parameters/_index.yaml#/parameters/issue"
    requestBody:
      content:
        application/json:
          schema:
            $ref: '../schemas/issues.yaml#/CloseIssueRequest'
      description: The request body.
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: '../schemas/issues.yaml#/Issue'
        description: Successful operation
    security: []
    tags:
      - Issues
AddComment:
  post:
    description: Adds a comment to a Issue.
    operationId: AddComment
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - $ref: "../parameters/_index.yaml#/parameters/issue"
    requestBody:
      content:
        application/json:
          schema:
            $ref: '../schemas/issues.yaml#/AddCommentRequest'
      description: The request body.
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: '../schemas/issues.yaml#/AddCommentResponse'
        description: Successful operation
    security: []
    tags:
      - Issues
ListComments:
  get:
    description: Lists comments for a Issue.
    operationId: ListComments
    parameters:
      - $ref: "../parameters/_index.yaml#/parameters/x-request-id"
      - $ref: "../parameters/_index.yaml#/parameters/provider"
      - $ref: "../parameters/_index.yaml#/parameters/issue"
      - description: 'Optional. The maximum number of comments to return. The service may return fewer than

          this value. If unspecified, at most 50 comments will be returned. The

          maximum value is 1000; values above 1000 will be coerced to 1000.'
        in: query
        name: pageSize
        schema:
          format: int32
          type: integer
      - description: 'Optional. A page token, received from a previous `ListComments` call.

          Provide this to retrieve the subsequent page.'
        in: query
        name: pageToken
        schema:
          type: string
    responses:
      default:
        content:
          application/json:
            schema:
              $ref: '../schemas/issues.yaml#/ListCommentsResponse'
        description: Successful operation
    security: []
    tags:
      - Issues
